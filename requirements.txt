# Core numerical and image-processing libraries
numpy>=1.24
Pillow>=10.0
tqdm>=4.66
scikit-learn>=1.3

# PyTorch and vector search
torch>=2.1
torchvision>=0.16
faiss-cpu>=1.8.0

# Vision-language model and Hugging Face utilities
transformers>=4.45,<5
accelerate>=0.30
qwen-vl-utils>=0.0.8
huggingface_hub>=0.25

# OpenAI CLIP (the project imports it as `clip`)
git+https://github.com/openai/CLIP.git

# Optional for NVIDIA CUDA evaluation only:
# flash-attn --no-build-isolation
